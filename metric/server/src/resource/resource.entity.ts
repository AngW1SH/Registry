export interface Resource {
  id: string;
  name: string;
  platform: string;
  project: string;
}

export interface ResourceDTO {
  id: string;
  name: string;
  platform: string;
  project: string;
}
