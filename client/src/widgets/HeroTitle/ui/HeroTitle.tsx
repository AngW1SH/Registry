import { FC } from "react";

interface HeroTitleProps {}

const HeroTitle: FC<HeroTitleProps> = () => {
  return <h1 className="text-7xl text-white">Реестр проектов</h1>;
};

export default HeroTitle;
