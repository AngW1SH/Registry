import { UserProjects } from "@/widgets/UserProjects";
import { FC } from "react";

interface UserProjectsPageProps {}

const UserProjectsPage: FC<UserProjectsPageProps> = () => {
  return <UserProjects />;
};

export default UserProjectsPage;
