import { UserRequests } from "@/widgets/UserRequests";
import { FC } from "react";

interface UserRequestsPageProps {}

const UserRequestsPage: FC<UserRequestsPageProps> = () => {
  return <UserRequests />;
};

export default UserRequestsPage;
